Place the trained PlantVillage model artifact here as `plant_disease_model.h5`.

The production model service expects this file path by default:
`python-app/ml_model/models/plant_disease_model.h5`

If you change the file name or location, set `MODEL_PATH` in the Render private service env vars.
